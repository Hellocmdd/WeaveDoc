<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:WeaveDoc.MarkdownEditor.Controls;assembly=WeaveDoc.MarkdownEditor"
        xmlns:vm="clr-namespace:WeaveDoc.MarkdownEditor.ViewModels;assembly=WeaveDoc.MarkdownEditor"
        x:Class="WeaveDoc.MarkdownEditor.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="WeaveDoc Markdown Editor" Width="1000" Height="700"
        MinWidth="600" MinHeight="400">
  <Grid RowDefinitions="Auto, Auto, *">
    <!-- 菜单 -->
    <Menu Grid.Row="0">
      <MenuItem Header="文件">
        <MenuItem Header="打开..." Click="OpenFile_Click" />
        <Separator />
        <MenuItem Header="保存" Click="SaveFile_Click" />
        <MenuItem Header="另存为..." Click="SaveAsFile_Click" />
      </MenuItem>
    </Menu>
    
    <TextBlock Grid.Row="1" Text="WeaveDoc Markdown Editor" FontSize="18" Margin="8"/>
    
    <!-- 左编辑器 / 右预览 布局 -->
    <Grid Grid.Row="2" ColumnDefinitions="*, *" Margin="8">
      <!-- 左侧编辑器：使用 Avalonia 内置的 TextBox 控件作为临时解决方案 -->
      <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <TextBox TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding EditorContent, Mode=TwoWay}" />
      </ScrollViewer>

      <!-- 右侧预览：显示由 MarkdownService 生成的 HTML（以文本形式展示） -->
      <ScrollViewer Grid.Column="1" Background="#FFF8F8F8" Padding="8" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <TextBlock TextWrapping="Wrap" Foreground="Black" FontSize="14" Text="{Binding Html}" />
      </ScrollViewer>
    </Grid>
  </Grid>
</Window>