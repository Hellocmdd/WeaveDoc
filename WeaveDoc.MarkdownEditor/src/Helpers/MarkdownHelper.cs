using System;

namespace WeaveDoc.MarkdownEditor.Helpers
{
    public static class MarkdownHelper
    {
        public static string ConvertToHtml(string markdown)
        {
            // This method will convert markdown text to HTML.
            // Implementation will depend on the markdown library used.
            throw new NotImplementedException("Markdown to HTML conversion is not implemented yet.");
        }

        public static string SanitizeHtml(string html)
        {
            // This method will sanitize the HTML to prevent XSS attacks.
            // Implementation will depend on the sanitization library used.
            throw new NotImplementedException("HTML sanitization is not implemented yet.");
        }
    }
}